<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" />
  </ItemGroup>
  <!--
    Referenced for HubEvents.All only: the harness subscribes to the generated inventory rather
    than a hand-written copy of it (#3318), so an event added to IGatewayHubClient is observable
    here without a second edit.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\src\extensions\BotNexus.Extensions.Channels.SignalR\BotNexus.Extensions.Channels.SignalR.csproj" />
  </ItemGroup>
</Project>