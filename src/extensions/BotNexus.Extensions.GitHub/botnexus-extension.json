{
  "id": "botnexus-github",
  "name": "GitHub",
  "description": "Platform-owned GitHub App installation credential. No agent tools in this slice.",
  "version": "1.0.0",
  "entryAssembly": "BotNexus.Extensions.GitHub.dll",
  "extensionTypes": ["tool"],
  "enabled": true,
  "configSchema": [
    {
      "id": "appId",
      "type": "string",
      "required": false,
      "sensitive": false,
      "description": "GitHub App id used as the JWT issuer when minting an installation token."
    },
    {
      "id": "installationId",
      "type": "string",
      "required": false,
      "sensitive": false,
      "description": "Installation id whose scoped access token is minted."
    },
    {
      "id": "privateKeyPath",
      "type": "string",
      "required": false,
      "sensitive": true,
      "description": "Filesystem path to the GitHub App PEM private key."
    },
    {
      "id": "apiBaseAddress",
      "type": "string",
      "required": false,
      "default": "https://api.github.com/",
      "sensitive": false,
      "description": "API base address. Override for GitHub Enterprise Server."
    },
    {
      "id": "expirySkewSeconds",
      "type": "integer",
      "required": false,
      "default": "60",
      "sensitive": false,
      "description": "Seconds before the reported expiry at which a cached token is refreshed."
    }
  ]
}
