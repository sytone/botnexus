# Dependencies for the BotNexus webhook sender example.
#
# The example ships two transport variants; install whichever you use (or both).
#
#   Async variant (httpx.AsyncClient):
httpx>=0.24
#
#   Sync variant (requests):
requests>=2.28
#
# HMAC signing uses only the Python standard library (hmac + hashlib) and needs
# no extra packages. Requires Python 3.9+.
#
# Install both:   pip install -r requirements.txt
# Install one:    pip install httpx        # async only
#                 pip install requests     # sync only
