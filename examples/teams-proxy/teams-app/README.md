# Teams app package

`manifest.json` is generated for the deployed Azure Bot:

- Teams app ID: `<teams-app-id-guid>`
- Bot client ID: `<bot-client-id-guid>`

Upload `botnexus-teams-proxy.zip` through Teams Developer Portal, Teams Admin Center, or app sideloading for a test tenant.
